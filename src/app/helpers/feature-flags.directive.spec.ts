import { FeatureFlags.Directive } from './feature-flags.directive';

describe('FeatureFlags.Directive', () => {
  it('should create an instance', () => {
    expect(new FeatureFlags.Directive()).toBeTruthy();
  });
});
