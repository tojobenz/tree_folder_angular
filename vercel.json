{
  "buildCommand": "export NODE_OPTIONS=--openssl-legacy-provider && npm run build",
  "devCommand": "export NODE_OPTIONS=--openssl-legacy-provider && npm run start",
  "installCommand": "npm install"
}
